<UserControl
    x:Class="Sphere_Schedule_App.Views.Controls.CustomTitleBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Height="48" Background="{StaticResource DarkBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- App Logo & Title -->
        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12" Margin="16,0">
            <Ellipse Width="32" Height="32">
                <Ellipse.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{StaticResource PrimaryBlueColor}" Offset="0"/>
                        <GradientStop Color="{StaticResource PrimaryGreenColor}" Offset="1"/>
                    </LinearGradientBrush>
                </Ellipse.Fill>
            </Ellipse>
            <TextBlock Text="Sphere Schedule"
                       Foreground="White"
                       FontSize="18"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Window Controls -->
        <StackPanel Grid.Column="2" Orientation="Horizontal">
            <!-- Search Button -->
            <Button x:Name="SearchButton"
                    Style="{StaticResource TitleBarButtonStyle}"
                    ToolTipService.ToolTip="Search">
                <FontIcon Glyph="&#xE721;" FontSize="14"/>
            </Button>

            <!-- Notifications -->
            <Button x:Name="NotificationsButton"
                    Style="{StaticResource TitleBarButtonStyle}"
                    ToolTipService.ToolTip="Notifications">
                <Grid>
                    <FontIcon Glyph="&#xEA8F;" FontSize="14"/>
                    <Border Background="Red"
                            CornerRadius="10"
                            Width="8"
                            Height="8"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Margin="0,-4,-4,0"/>
                </Grid>
            </Button>

            <!-- Profile -->
            <Button x:Name="ProfileButton"
                    Style="{StaticResource TitleBarButtonStyle}"
                    ToolTipService.ToolTip="Profile">
                <Ellipse Width="24" Height="24">
                    <Ellipse.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="{StaticResource AccentPurpleColor}" Offset="0"/>
                            <GradientStop Color="{StaticResource AccentPinkColor}" Offset="1"/>
                        </LinearGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>